name: check app status
on:
  workflow_dispatch:

jobs:
  test_status:
    runs-on: ubuntu-latest
    steps:
      - name: check app status
        run: |
          echo ${{ env.MESSAGE1 }}
          echo ${{ secrets.ACC_KEY1 }}
